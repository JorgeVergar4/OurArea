package cl.duoc.ourarea.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de la app
object AppColors {
    // Primary - Teal principal
    val Primary = Color(0xFF19B6B6)
    val PrimaryVariant = Color(0xFF00BFA5)
    val PrimaryDark = Color(0xFF00796B)

    // Secondary
    val Secondary = Color(0xFF19B6B6)
    val SecondaryVariant = Color(0xFF1A9999)

    // Backgrounds
    val Background = Color(0xFFF4FBF8)
    val BackgroundGray = Color(0xFFF5F7FA)
    val Surface = Color.White
    val CardWhite = Color.White

    // Teal variants
    val LightTeal = Color(0xFFE0F7FA)
    val MediumTeal = Color(0xFF80CBC4)

    // Text colors
    val TextPrimary = Color(0xFF1A2524)
    val TextSecondary = Color(0xFF666666)
    val TextGray = Color.Gray
    val TextDisabled = Color(0xFFBDBDBD)

    // Border colors
    val BorderLight = Color(0xFFE0E5EA)
    val BorderFocused = Color(0xFF19B6B6)

    // Status colors
    val Success = Color(0xFF4CAF50)
    val Error = Color.Red
    val Warning = Color(0xFFFFA726)
    val Info = Color(0xFF2196F3)

    // Other
    val Transparent = Color.Transparent
    val Divider = Color(0xFFE0E0E0)
}
