package cl.duoc.ourarea.ui.theme

import androidx.compose.ui.graphics.Color

// Colores principales de la app - Estética OurArea
object AppColors {
    // Primary - Turquesa OurArea
    val Primary = Color(0xFF19B6B6) // Turquesa principal
    val PrimaryDark = Color(0xFF0D6B6B)

    // Secondary - Verde Google
    val Secondary = Color(0xFF34A853) // Google Green
    val SecondaryVariant = Color(0xFF2E7D32)

    // Backgrounds - Estilo suave
    val Background = Color(0xFFF4FBF8) // Fondo suave verde-azul
    val BackgroundGray = Color(0xFFF5F5F5)
    val Surface = Color.White

    // Google Colors
    val GoogleBlue = Color(0xFF4285F4)
    val GoogleRed = Color(0xFFEA4335)
    val GoogleYellow = Color(0xFFFBBC04)
    val GoogleGreen = Color(0xFF34A853)

    // Text colors - Google Style
    val TextPrimary = Color(0xFF202124) // Google Text Primary
    val TextSecondary = Color(0xFF5F6368) // Google Text Secondary
    val TextGray = Color(0xFF9AA0A6)
    val TextOnPrimary = Color.White

    // Border colors
    val BorderLight = Color(0xFFDADCE0) // Google Border

    val Error = Color(0xFFEA4335)

    // Map specific colors
    val MapCardBackground = Color(0xFFFFFFFF)

    val Divider = Color(0xFFE8EAED)
}
